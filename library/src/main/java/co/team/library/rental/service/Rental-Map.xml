<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="co.team.library.rental.service.RentalMapper">
	<select id="allMemberRentalList"
		resultType="co.team.library.rental.vo.RentalVO">
		SELECT * FROM RENTAL ORDER BY RENTALDATE DESC
	</select>

	<select id="myRentalList" parameterType="String"
		resultType="co.team.library.rental.vo.RentalVO">
		SELECT ID, A.BOOKTITLE AS BOOKNAME, RENTALDATE, RETURNDATE,
		RETURNORNOT
		FROM BOOKLIST A INNER JOIN RENTAL B
		ON A.BOOKCODE =
		B.BOOKCODE
		WHERE ID = #{id}
		ORDER BY RENTALDATE DESC
	</select>

	<select id="nowRentalList" parameterType="String"
		resultType="co.team.library.rental.vo.RentalVO">
		<![CDATA[
		SELECT ID, A.BOOKTITLE AS BOOKNAME, RENTALDATE, RETURNDATE, RETURNORNOT
		FROM BOOKLIST A INNER JOIN RENTAL B
		ON A.BOOKCODE = B.BOOKCODE
		WHERE ID = #{id}
		AND B.RENTALDATE <= SYSDATE
        AND SYSDATE <= B.RETURNDATE
        AND B.RETURNORNOT != 1	
        ORDER BY RENTALDATE DESC
        ]]>
	</select>

	<select id="memberRentalList"
		resultType="co.team.library.rental.vo.RentalVO">

		SELECT *
		FROM (
		SELECT ROWNUM RN, A.*
		FROM (
		SELECT * FROM RENTAL ORDER BY RENTALDATE ASC
		) A
		)
		WHERE RN BETWEEN #{start} AND #{end}
	</select>

	<select id="boardListCount3" resultType="int">
		SELECT COUNT(*) FROM
		RENTAL
	</select>
</mapper>
